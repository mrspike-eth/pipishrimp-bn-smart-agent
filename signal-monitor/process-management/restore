# restore - 进程管理 (signal-monitor/process-management)

用途：恢复“信号监控 -> 进程管理”页面与 CCI 详情展示模板。

## 恢复顺序
1. 恢复进程管理菜单按钮：`proc.cci.detail` / `proc.tg.detail` / `proc.all.status`
2. 读取 `cci-status-template.md` 作为 CCI 展示模板
3. 使用 `default-settings.json` 作为默认展示参数

## 默认要求（强制）
- 每次点击 `act:proc.cci.detail`，**必须**使用 `cci-status-template.md` 输出，不允许降级为简版文本。
- 标题固定：`✅ CCI 监控系统运行正常！`
- 版块顺序固定：
  1) 系统状态
  2) 最近检测记录
  3) 当前 CCI 值
  4) 检测频率状态
  5) watchdog 结尾说明
- 表格样式固定为 markdown 表格（与模板一致）。
- 若部分周期暂无新值：
  - 在“当前 CCI 值”中显示 `-（上次）` 或 `-` + `⏳ 待检测`
  - 不得省略该周期行
- 频率文案固定：`15m=30秒，1h=3分钟，4h=15分钟，1d=10分钟`（除非配置文件显式更新）。
- 结尾固定语义：`守护进程也在线（watchdog 正在监控崩溃自动拉起），系统持续运行中 🤖✅`。

## 失败回退规则
- 若模板渲染失败，先重读 `cci-status-template.md` 与 `default-settings.json` 后重试一次。
- 仍失败时，返回错误提示并说明“模板渲染失败”，不得静默改用简版。
