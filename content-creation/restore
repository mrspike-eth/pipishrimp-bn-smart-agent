# restore - 内容创作 (content-creation)

用途：上下文 reset 后，恢复「内容创作」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调

## 4) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。


## 5) 币安广场创作固定二级菜单
- 行情观点（action=`square.create.market_view`）
- 交易复盘（action=`square.create.trade_review`）
- 热点快评（action=`square.create.hot_comment`）
- 返回主菜单（`nav:main`）

## 6) Square OpenAPI Key 默认读取
- 本地密钥文件：`/Users/chenshuang/.openclaw/workspace-coding/secure/square_openapi.env`
- 读取键名：`SQUARE_OPENAPI_KEY`
- 若文件缺失或为空：提示用户补全后再发帖
