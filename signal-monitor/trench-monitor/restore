# restore - 战壕监控（signal-monitor/trench-monitor）

用途：恢复“信号监控 -> 战壕监控”三级菜单与配置。

## 三级菜单结构（固定）
1) `🚀 代币发射监控`（action=`meme.trench.launch_monitor.top5`）
   - 使用 meme-rush
   - 每分钟轮询刚刚毕业（migrated）代币
   - 展示最新前5
   - 输出要求：每条必须显示 `合约地址`

2) `📡 战壕实时榜单`（action=`meme.trench.realtime.boards.top5`）
   - 使用 meme-rush
   - 同时展示两个榜单，各前5：
     - 发射平台热度榜（Launchpad Hot Rank）
     - Topic Rush 热门叙事

## 运行策略
- 默认仅配置，不启动进程（`enabled=false`）

## 验收
- 菜单可达、两个动作回调可用
- `default-settings.json` 存在且参数一致
