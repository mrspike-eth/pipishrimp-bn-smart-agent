# restore - BN Wallet链上交易 (bn-wallet-onchain-trade)

用途：上下文 reset 后，恢复「BN Wallet链上交易」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调


## 4) 二级菜单结构（强制）
- 钱包资金（action=`onchain.wallet.funds`）
- 持仓币种（action=`onchain.wallet.positions`）
- 交易记录（action=`onchain.wallet.trades`）
- pnl收益情况（action=`onchain.wallet.pnl`）
- 额外按钮：刷新数据（`onchain.wallet.refresh`）/ 返回主菜单（`nav:main`）
- 数据驱动：`bnbchain-mcp`

## 5) 参考配置
- `default-settings.json`

## 6) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。
