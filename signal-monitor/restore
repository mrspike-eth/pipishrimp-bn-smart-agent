# restore - 信号监控 (signal-monitor)

用途：上下文 reset 后，恢复「信号监控」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调

## 4) 榜单子菜单恢复规则（新增）
- 二级菜单必须包含：`📊 币安市场币种榜单`（next=`market_rank`）。
- 点击 `nav:market_rank` 时必须直接输出完整5段榜单（前5+交叉信号），不要求二次点击。
- `market_rank` 节点下固定 5 个动作入口：
  1. 社交热度榜（前5）→ `cmr.social_hype.top5`
  2. 统一代币榜（前5）→ `cmr.unified.top5`
  3. 聪明钱净流入榜（前5）→ `cmr.smart_inflow.top5`
  4. Meme 榜（前5）→ `cmr.meme.top5`
  5. 交叉信号榜 → `cmr.cross_signal.top`
- 交叉信号榜定义：以上四个榜单中出现重叠的币种（按 symbol+contractAddress 归一），按出现次数降序展示。

## 5) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。

- 二级菜单新增：`📍 地址监控`（next=`address_monitor`），三级动作 `cmr.address_pnl.top10` 固定返回前10地址PnL榜。
- 二级菜单新增：`🪖 战壕监控`（next=`trench_monitor`），含两个三级动作：`代币发射监控`（毕业代币前5）与 `战壕实时榜单`（发射平台热度榜前5 + Topic Rush前5），默认仅配置不启动。
- 地址榜单默认格式：每个地址显示“胜率+Realized PnL+短地址+收益前三币种”自然语言块。
