# restore - 智能投资助理 (smart-investment-assistant)

用途：上下文 reset 后，恢复「智能投资助理」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调



## 4) 三级菜单结构（强制）
- 钱包地址分析（next=`invest_wallet_analysis`）
  - 动作：`invest.wallet.analyze.prompt`
  - 行为：先询问地址，再默认调用 `query-address-info` 输出报告
- 币种分析（next=`invest_contract_analysis`）
  - 动作：`invest.contract.analyze.prompt`
  - 行为：先询问合约地址，再默认调用 `query-token-audit` + `query-token-info` + `opentwitter-mcp` 输出完整报告
  - 报告第4项固定为“推特叙事雷达”（按 `twitter-narrative-radar-template.md` 结构生成）
- surf高级投研（action=`invest.surf.research.prompt`）
  - 行为：询问项目后，调用 asksurf 前端流程生成并回传完整研报

## 5) 参考配置
- `submenus.json`
- `default-settings.json`

## 6) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。
