# restore - 地址监控（signal-monitor/address-monitor）

用途：恢复“信号监控 -> 地址监控 -> 币安高收益地址榜单”。

## 固定行为
- 二级入口：`📍 地址监控`（next=`address_monitor`）
- 三级动作：`🏆 币安高收益地址榜单`（action=`cmr.address_pnl.top10`）
- 点击后固定返回 Address PnL 榜前10（自然语言列表/表格均可，但必须是前10）。

## 数据源
- 技能：`crypto-market-rank`
- API: Address PnL Rank
- 默认参数：chainId=CT_501, period=30d, tag=ALL, pageNo=1, pageSize=10

## 验收
- `nav:address_monitor` 可达
- `cmr.address_pnl.top10` 返回10条地址排行
- 不要求用户二次补参数


## 固定输出格式（已确认）
- 每个地址按以下格式输出：
  `1) 名称（胜率：xx.xx%、Realized PnL：x,xxx.xx）`
  `地址：xxxx...xxxx`
  `收益前三币种：`
  `• TokenA：value`
  `• TokenB：value`
  `• TokenC：value`
- 若地址标签为空，名称显示“未命名”。
- 地址显示短地址（前4~5位 + ... + 后4位）。
- 该格式为默认且强制，除非用户另行指定。
