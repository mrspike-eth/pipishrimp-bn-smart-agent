# reset-restore

目标：在上下文重置（/reset、/new、长会话压缩）后，按固定流程恢复到 reset 前的菜单、配置和操作状态。

## 0. 恢复原则
- 单一事实源：本仓库文件。
- 先恢复菜单，再恢复各业务模块。
- 菜单回调统一使用 `nav:`（跳转）和 `act:`（动作）。

## 1. Step-1（必须先做）：从 settings 开始恢复菜单
优先路径：`settings/menu-management/`

1) 读取 `settings/menu-management/visual-menu.json`（完整一级/二级/三级菜单真相）。
2) 读取 `settings/menu-management/menu-root.json`（一级菜单按钮顺序）。
3) 读取 `settings/menu-management/menu-nodes.json`（二级/三级节点与 action 参数）。
4) 读取 `settings/menu-management/skill-list-panel.json`（Skill 面板布局，必须是 grouped-vertical）。

恢复标准：
- 一级菜单 10 项完整显示：
  - 币安日报 / 币种自选列表
  - 币安现货交易 / BN Wallet链上交易
  - 信号监控 / 智能投资助理
  - 内容创作 / 学习模式
  - Skill技能管理 / 设置
- `BN Wallet链上交易` 不可缩写。
- 回调前缀必须为 `nav:`、`act:`。

## 2. Step-2：逐目录恢复一级菜单对应配置
按下列目录顺序恢复（每个目录中的 `menu-config.json` 为准）：
1. `binance-daily/`
2. `watchlist/`
3. `binance-spot-trade/`
4. `bn-wallet-onchain-trade/`
5. `signal-monitor/`
6. `smart-investment-assistant/`
7. `content-creation/`
8. `learning-mode/`
9. `skill-management/`
10. `settings/`

说明：
- 若目录存在 `submenus.json`，则必须同步恢复其全部二级/三级子节点。
- 不允许只恢复文本，不恢复按钮回调。

## 3. Step-3：恢复技能视图与分组
- 读取 `skill/skills-config.json`。
- 保证“已安装技能”使用分组纵向聚合显示（同组纵向连续，不跨组混排）。

## 4. Step-4：状态检查清单
- 一级菜单 10 项是否齐全。
- `nav/act` 回调是否全部可达。
- Skill 分组是否符合 grouped-vertical。
- 关键策略是否保留：聪明钱监控默认不自动启动，按需手动启动。

## 5. Step-5：恢复后交互建议
- 先发送主菜单按钮。
- 再按用户点击回放二级/三级菜单。
- 若用户要求“全面恢复”，执行全链路点测并输出“缺失项=0/问题列表”。
