# restore - 币种自选列表 (watchlist)

用途：上下文 reset 后，恢复「币种自选列表」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调

## 4) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。

## 5) 自选列表展示规则（固定）
- 进入 `nav:watchlist` 时自动展示当前关注币种（button-first + 文本列表）。
- 每行一个币种，格式：
  `币种名称 市值： 流动性： 24h交易量： 持有者：`
- 运行时列表文件：`/Users/chenshuang/.openclaw/workspace-coding/secure/watchlist_tokens.json`

## 6) 添加币种规则（固定）
- 新增资产必须先走「智能投资助理 -> 币种分析」流程。
- 分析数据源要求：`query-token-info` + `query-token-audit`。
- 分析完成后再写入 watchlist 运行时文件。
