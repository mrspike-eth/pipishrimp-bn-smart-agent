# restore - 币安现货交易 (binance-spot-trade)

用途：上下文 reset 后，恢复「币安现货交易」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调

## 4) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。

- `spot_trade` 二级菜单固定按钮：账户资产（spot.account.assets）｜仓位（spot.positions）｜交易（spot.trade）
