# restore - 币安日报 (binance-daily)

用途：上下文 reset 后，恢复「币安日报」菜单及关联功能。

## 1) 读取配置
- `menu-config.json`（当前一级菜单节点）
- `submenus.json`（若存在，恢复全部二/三级菜单）

## 2) 恢复动作
1. 按 `menu-config.json` 发送该菜单节点按钮（必须保留 `nav:/act:` 回调）。
2. 若存在 `submenus.json`，将其子节点也同步恢复（不可只恢复文本）。
3. 若本菜单包含可执行功能，优先先返回状态，再执行动作。

## 3) 验收标准
- 节点文案与按钮顺序与配置一致
- 回调可达、可继续跳转
- 不出现临时占位回调

## 4) 固定规则（强制）
- `nav:daily` 必须返回“三日报按钮版”，禁止纯文本兜底。
- 每次 `nav:daily` 响应必须使用 Telegram 按钮（inline keyboard）渲染，不允许文本列表。
- 固定按钮：
  1) `🟨 币安广场日报` → `act:daily.square`
  2) `🟦 币安活动日报` → `act:daily.activity`
  3) `🟪 推特日报` → `act:daily.twitter`
  4) `⬅️ 返回主菜单` → `nav:main`
- 若渲染失败：先重读 `default-settings.json` 与 `report-recovery-template.md` 重试一次；仍失败要显式报错，不允许降级旧简化菜单。
- `act:daily.square` 输出逻辑固定：
  1) 使用三段式固定模板（热门文章 / 热点话题 / Social Hype 币种）；
  2) TOP10文章必须仅显示“标题”（不展示简介），标题最长120字，超出省略；
  3) 热门讨论币种 Top10 必须竖向逐条输出（N. 币种（热度值）），禁止单行横排。

## 5) 参考资料
- `default-settings.json`
- `report-recovery-template.md`

## 6) 备注
- 此文件仅定义恢复步骤，不保存运行时秘密。
